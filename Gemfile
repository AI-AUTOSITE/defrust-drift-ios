source "https://rubygems.org"

# Pinned to 2.235.0: fastlane 2.236.0 has a regression where upload_to_testflight
# fails on Xcode 26 with "The file couldn't be opened because it isn't in the
# correct format. (259)" (fastlane issue #30065, fix pending in PR #30066).
gem "fastlane", "2.235.0"
